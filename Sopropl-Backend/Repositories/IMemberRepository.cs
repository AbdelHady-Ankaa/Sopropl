using System.Threading.Tasks;
using Sopropl_Backend.Models;

namespace Sopropl_Backend.Repositories
{
    public interface IMemberRepository
    {
        // Task<Member> FindMemberInOrganizationAsync(Organization organization, User user);
        // Task<Member> FindMemberInTeamAsync(Organization organization, Team team, string memberId);
        // Task<Member> CreateAsync(Organization organization, User user);
    }
}